public class Main {
    public static void main(String[] args) {
        String[] arr = {"apple", "banana", "cherry", "date", "elderberry", "fig", "grape"};
        Main main = new Main();
        String[] sortedArr = main.sortStrings(arr);
        for (String str : sortedArr) {
            System.out.println(str);
        }

    }

    public String[] sortStrings(String[] arr) {

        // given an array of strings, implemet a method that sorts the array in ascending order by the second element inside the strings and returns the sorted array




    }
}